# Personal workflow completion

User intent: prepare routines at home, import AI-authored programs easily, inspect today's exercise technique and notes, pause/resume without lost work, hear rest alerts, correct forgotten duration, inspect attendance/load progression/records, record nutrition and measurements. Preserve established Hevy/Strong/StrengthLog patterns and Overload styling.

Authorized direction: extend the existing workflows, not redesign navigation. Use an additive program import in Train with a downloadable AI instruction/schema/catalog kit, file or pasted JSON input, validation and readable preview, then one explicit import. Never use full backup restore for an AI plan. Resolve exercise names against the real catalog; never silently substitute an ambiguous exercise. Import must be atomic locally, isolated to current account, safe on retry, and preserve history/settings.

Existing workout duration editor, pause/persistence, exercise technique/journal and statistics require scenario verification. Alerts must describe actual platform behavior; a PWA callback cannot promise delivery while its process is suspended. Phone platform clarification is pending. Nutrition now supports eight daily nutrient totals and past-date editing as the common base. User clarification is still pending for a separate food/meal diary versus keeping daily totals only.

User has already authorized autonomous implementation and routine design choices. Questions concern actual scope/platform, not redundant approval. No external paid AI API, hosted notification service, account registration, or publication without an applicable user decision.
